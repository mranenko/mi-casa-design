{
  "devDependencies": {
    "live-server": "^1.2.2",
    "sass": "^1.63.6"
  },
  "engines": {
    "node": "18.16.0",
    "npm": "9.5.1"
  },
  "scripts": {
    "sass": "sass --watch styles",
    "server": "live-server --open=system/index.html --port=20237",
    "start": "npm run sass & npm run server"
  }
}
